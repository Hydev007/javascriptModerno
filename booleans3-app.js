const autenticado = true;

if(autenticado === true){
    console.log("puede pasar")
} else {
    console.log("no puede pasar")
}

if(autenticado){
    console.log("puede pasar")
} else {
    console.log("no puede pasar")
}

//operador ternario

console.log(autenticado ? "puede pasar" : "no puede pasar");